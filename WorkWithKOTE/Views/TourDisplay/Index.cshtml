@{
    ViewBag.Title = "Тур";
}
@using WorkWithKOTE.Models
@model  WorkWithKOTE.Models.TourForDisplaing

<div class="primaryContainer clearfix">
    @{ Html.RenderPartial("HeaderPartial_main");}
    <div id="big-preview-img" class="clearfix" style="background: url('@Html.DisplayFor(m => m.TourForDisplay.TourImg)') no-repeat center 100%; background-size:100% 850px;">
        <div class="tour-name">@Html.DisplayFor(m => m.TourForDisplay.NameTour)</div>

        <div class="bottom">
            <div class="align_center_to_left">
                <div class="align_center_to_right">
                    <div class="short-description">
                        <p class="tour-date">25.06.2015 - 28.06.2015</p>

                        <p class="tour-destination">@Html.DisplayFor(m => m.TourForDisplay.PlaceOfDeparture) -> @Html.DisplayFor(m => m.TourForDisplay.PlaceOfArrival)</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="booking">
            <div id="description">
                <p>Описание</p>
            </div>

            <div class="left-column">
                <p>Стоимость:</p>

                <p>
                    @if (Model.TourForDisplay.AukcionPrice != null)
                    {
                        <span class="old-price">@Model.TourForDisplay.Cost @Model.TourForDisplay.Valuta</span> <span class="new-price">@Model.TourForDisplay.AukcionPrice @Model.TourForDisplay.Valuta</span>
                    }
                    @Html.DisplayFor(m => m.TourForDisplay.PodpicePrice);
                </p>

                @*                <p>Тип тура: <a href="">Семейный отдых</a>, <a href="">бизнес отдых</a></p>*@
                @if (Model.TourForDisplay.Bonus != null)
                {
                    <p>
                        Бонус за тур :@Html.DisplayFor(m => m.TourForDisplay.Bonus)
                    </p>
                }
                <p>
                    Теги тура:
                    @foreach (var item in Model.TagForDisplay)
                    {@Html.DisplayFor(m => item.TagName)
                    }
                </p>
                <p>
                    Транспорт:
                    @if (Model.TourForDisplay.IsTrain)
                    {
                        <img src="~/Content/img/t-train.png" style="display: inline-block; width: 25px; height: 25px;">
                    }
                    @if (Model.TourForDisplay.IsShip)
                    {
                        <img src="~/Content/img/t-ship.png" style="display: inline-block; width: 25px; height: 25px;">
                    }
                    @if (Model.TourForDisplay.IsBus)
                    {
                        <img src="~/Content/img/t-bus.png" style="display: inline-block; width: 25px; height: 25px;">
                    }
                    @if (Model.TourForDisplay.IsAriplane)
                    {
                        <img src="~/Content/img/t-plane.png" style="display: inline-block; width: 25px; height: 25px;">
                    }
                </p>

                <p>
                    Дополнительные услги: @foreach (var item in Model.DopUslugForDisplay)
                    {
                        @Html.DisplayFor(m => item.Name) @:,
 }
                </p>

                <span>Количество участников:  @Html.DisplayFor(m => m.TourForDisplay.AllPeople) ( Уже едут: @Html.DisplayFor(m => m.TourForDisplay.People) )</span>
            </div>
            <div class="right-column">
                <p>Если у Вас есть вопросы,обратитесь к нашему консультанту:</p>

                <p><span class="telephone">+38 097-815-68-38</span> <span class="telephone">+38 050-774-99-18</span></p>

                <p><a href="">Обратный звонок</a></p>
            </div>
            <div class="buttons">
                <a href="@Url.Action("Index", "Buy", new { id = Model.TourForDisplay.TourId })" id="book-and-pay">Бронь и оплата</a>
                @*                <a href="#" id="book-tour">Предварительная бронь</a>*@
                @if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
                {
                    <a href='@Url.Action("TourEdit", "TourCreate", new {id = Model.TourForDisplay.TourId})' id="book-tour">Редактировать тур</a>

                }
            </div>
        </div>
        <div class="description">
            <div class="right-panel">

                <div class="guide">
                    <h4>Сопровождающий</h4>
                    <img src="@Html.DisplayFor(m => m.TourForDisplay.SuppFoto)" class="img-photo guide-photo">

                    <div class="person-name"><a href="@Html.DisplayFor(m => m.TourForDisplay.SuppVkontakte)">@Html.DisplayFor(m => m.TourForDisplay.SuppName)</a></div>
                    @*                    <div class="position">Директор туристического оператора КОТЕ</div>*@
                    <div class="about-person">
                        @Html.DisplayFor(m => m.TourForDisplay.SuppDiscription)
                    </div>
                </div>

                <div class="social">
                    <div class="tourists">
                        <p>Уже едут:</p>
                        <a href="#"><img src="~/Content/img/10942586.jpg" class="tourist"></a>
                        <a href="#"><img src="~/Content/img/10942586.jpg" class="tourist"></a>
                        <a href="#"><img src="~/Content/img/10942586.jpg" class="tourist"></a>
                        <a href="#"><img src="~/Content/img/10942586.jpg" class="tourist"></a>
                        <a href="#"><img src="~/Content/img/10942586.jpg" class="tourist"></a>
                    </div>

                    <div class="similar-tour">
                        <p>Похожие туры:</p>
                        <a href="#">Летний отдых в Батуми</a>
                        <a href="#">Грузинские приключения</a>
                    </div>
                    <div class="">
                        <p>Поделиться:</p>
                    </div>
                    <ul>
                        @foreach (var item in Model.TagForDisplay)
                        {
                            <li>@Html.DisplayFor(m => item.TagName)</li>
                        }
                    </ul>

                </div>
            </div>
            <div class="full-description">
                <h1>Программа тура:</h1>
                @Html.Raw(Model.TourForDisplay.DescriptionTour)
                <div id="map_canvas" style="width: 90%; height: 300px">
                    <h1>Карта путешествия:</h1>
                    @{Html.RenderAction("MapForDisplay", new { id = Model.TourForDisplay.TourId });}
                </div>
            </div>
        </div>
        @section scripts{
        }
    </div>
</div>